# nove 项目书：个人/组织/企业数据仓库与 Agent 数据基础设施

- 版本：v2.0（草案）
- 定位：面向 AI 时代的智能数据仓库
- 文档状态：评审中
- 作者：项目组（杨仕明/叶俊/明轩 等）

## 1. 项目概述

### 1.1 背景

nove 项目致力于构建一个**面向个人、组织与企业的智能数据仓库**，作为 AI 时代组织的数据底座与 Agent 数据基础设施。系统实时、定时地从方方面面获取数据（会议、订单、用户、聊天记录、客户对接、用户画像、群聊、产品、邮件、合同等），经 AI 实时预处理与关联分析，形成组织庞大的**有机结合的数据仓库**，并通过 **API / MCP / Skill / CLI** 等一系列接口对外提供数据交互。

数据仓库的主要服务对象是 **AI 与 Agent**（作为其他 Agent Harness 的强大数据后盾），人类用户主要通过 **nove-admin** 查看与操作。整个体系对 AI 实行**严格且全面的权限访问控制**。

首个落地客户为 **陆向谦实验室**（教育场景：会议 / 订单 / 用户数据）。在完成实验室级验证后，逐步拓展至教育机构、高校、企业培训及更广泛的个人/组织/企业场景。

### 1.2 目标

- 建设组织级数据仓库：多源数据实时/定时汇聚、统一存储、权限隔离。
- AI 实时预处理：自动总结、字段映射、实体抽取；自动发现数据间的关联与联系，形成有机结合的数据仓库。
- 多协议开放：通过 API、MCP、Skill、CLI 等接口对外提供数据交互，服务 AI、Agent 与第三方系统。
- 严格的人机权限：AI 以独立身份接入，最小授权、全量审计，AI 只能在授权范围内访问数据。
- 可拓展数据格式：内置数据仓库格式，支持以自定义方式（插件或其他形式）接入新数据形态。
- 交付最小可用应用（nove-admin，Web），作为人类查看与操作入口。

### 1.3 建设原则

- 以人为本：AI 增强而非替代，人类保留最终判断权。
- 价值优先：围绕真实使用场景交付最小可用版本（MVP）。
- 渐进迭代：小步快跑、连续集成，数据与能力持续进化。
- 可替换性：模块化边界清晰，技术栈可演进、实现可替换。
- 权限为本：数据访问（尤其是 AI 访问）始终受控、可审计。
- 扁平协作：信息直达、减少流程摩擦，强调交付与反馈闭环。

---

## 2. 需求与范围

### 2.1 核心用户

- **AI / Agent**：通过 API / MCP / Skill / CLI 读取数据、执行任务（主要消费者）。
- **数据提供方**：会议、订单、聊天、邮件、合同等外部系统，向仓库推送/被拉取数据。
- **人类用户（管理员 / 业务人员）**：通过 nove-admin 查看数据、配置权限、管理数据源、审计 AI 行为。
- **开发者 / 集成方**：接入新数据源（插件/自定义格式）、构建基于仓库的 Agent 应用。

### 2.2 典型场景

- **多源数据汇聚**：会议记录、订单、用户、聊天记录、客户对接、群聊、产品、邮件、合同等实时/定时入库。
- **AI 预处理与关联**：自动总结参会记录、抽取实体、建立"用户 ↔ 会议 ↔ 订单 ↔ 邮件"等关联，形成语义层。
- **Agent 数据调用**：外部 Agent 通过 MCP / Skill / API 按权限读取仓库数据，完成总结、分析、报告等任务。
- **人类管理操作**：管理员通过 nove-admin 查看数据全景、管理数据源与权限、审计 AI 访问。
- **自定义接入**：新数据形态通过插件或自定义格式接入，扩展仓库能力。

### 2.3 范围边界（v1.0）

- 包含：核心数据源接入（会议、订单、用户）、基础 AI 预处理、API / MCP / Skill / CLI 接口、人机统一权限与审计、nove-admin 查看与操作、运营看板雏形。
- 暂不包含：移动端/桌面端、复杂低代码平台、跨组织数据联邦（可于 v1.x 规划）。

---

## 3. 总体架构

### 3.1 系统四层架构

```
[数据源层]
  - 会议（腾讯会议/飞书会议 Webhook + API）
  - 订单 / 用户 / 产品（业务系统）
  - 聊天 / 群聊 / 邮件 / 合同（IM、邮件、文档系统）
  - 其它云文档与非结构化文本

        │ 实时 / 定时拉取 · Webhook 订阅 · ETL 清洗/脱敏
        ▼
[数据与索引层]
  - 关系库（PostgreSQL + Prisma：主数据/权限/审计/元数据）
  - 向量库（pgvector / Milvus：嵌入、语义检索）
  - 版本 / 溯源与元数据管理；内置数据仓库格式

        │ AI 预处理 · 关联发现 · RAG
        ▼
[能力与中台层]
  - AI 预处理（总结/抽取/映射）
  - 关联引擎（数据关联、知识图谱、语义层）
  - 智能体能力（RAG 问答、报告生成、任务执行）
  - 人机统一权限（RBAC + ABAC + AI 独立身份 + 审计）

        │ API / 网关
        ▼
[接口与应用层]
  - REST API / GraphQL / MCP / Skill / CLI
  - nove-admin（人类查看与操作）
  - SDK / 接口文档（OpenAPI 契约驱动）
```

### 3.2 核心模块详解

#### 多源数据接入（Connector）
1. **数据源抽象**：统一连接器接口，支持 OAuth / API Key 等认证方式。
2. **同步策略**：实时（Webhook / 流）与定时（增量拉取 / 变更检测）结合。
3. **统一处理**：结构化与非结构化数据的统一入库流程（元数据提取、权限映射、冲突解决）。
4. **自定义扩展**：插件化接入新数据源 / 新数据格式。

#### AI 预处理与关联引擎
1. **实时预处理**：数据入库时自动总结、抽取实体、映射字段（轻量任务）。
2. **关联发现**：批量分析数据间关联（Embedding、图谱构建、跨源关联），形成"有机结合的数据仓库"（重量任务）。
3. **语义层**：统一检索、推理、会话记忆，支撑 RAG 问答与 Agent 调用。

#### 人机统一权限体系
1. **双主体模型**：人类用户与 AI Agent 均为独立访问主体。
2. **权限模型**：RBAC 为主 + ABAC 场景补充；AI 按 scope 白名单授权。
3. **全链路审计**：人类与 AI 的每次访问均可追溯、可回放。
4. **数据脱敏**：自动识别敏感信息（手机号、身份证号）并模糊化处理。

#### 多协议接口层
1. **REST / GraphQL**：标准数据访问接口。
2. **MCP（Model Context Protocol）**：作为 MCP Server 暴露，供 LLM 直接理解与调用。
3. **Skill**：面向 Agent 的技能化封装（如 nove-skills）。
4. **CLI**：命令行数据访问（nove-cli）。

---

## 4. 关键功能

### 4.1 多源数据汇聚
- 会议：腾讯会议 / 飞书会议 Webhook + API，自动沉淀转写 / 摘要 / 关键词 / 行动项。
- 业务：订单、退款、用户、产品、渠道等实时同步。
- 沟通：聊天记录、群聊、客户对接、邮件、合同等非结构化数据入库。
- 异步任务处理：消息队列（Redis / BullMQ）处理长时任务。

### 4.2 AI 预处理与关联
- 自动总结：参会记录、聊天记录、邮件等自动生成摘要。
- 实体抽取：人、组织、项目、产品等实体识别与关联。
- 数据关联：跨数据源建立关联（如"用户 ↔ 会议 ↔ 订单 ↔ 邮件"），形成语义层与知识图谱。

### 4.3 RAG 问答与检索
- 多轮会话：支持上下文连续对话，维持会话记忆。
- 权限感知：基于角色与 AI scope 的精准检索。
- 可追溯引用：提供答案来源链接，支持事实核验。
- 质量保证：幻觉检测、答案评分、不确定性提示。

### 4.4 智能体编排与开放
- 模板化配置：按部门 / 场景预设智能体模板。
- 数据源绑定：为 Agent 指定可访问的数据范围。
- API 封装：为第三方系统与 Agent Harness 提供标准化接口。

### 4.5 权限与合规系统
- 身份认证：OAuth 2.0、JWT、API Key（含 `sk_` 前缀的长期密钥）等多种方式。
- 权限模型：RBAC 为主 + ABAC 场景补充；AI 独立身份与 scope。
- 数据脱敏：自动识别敏感信息并模糊化处理。
- 全链路审计：人类与 AI 的操作留痕与溯源。
- 存储安全：敏感数据加密、密钥托管、备份策略。

### 4.6 运营监控与数据分析
- 实时指标：数据接入量、接口调用量、AI 任务成功率、权限审计事件。
- 智能分析：数据质量、关联覆盖率、热门数据访问识别。
- 预警机制：系统性能、错误率、安全威胁的智能监控与通知。

---

## 5. 技术方案与选型

### 5.1 推荐技术架构

#### 主业务后端 nove-api
- **框架**: NestJS + TypeScript（模块化单体，强类型）
- **数据存储**:
  - PostgreSQL + Prisma ORM（主数据 / 权限 / 审计 / 元数据）
  - Redis（缓存 / 会话 / 队列）
  - 向量库（pgvector 起步 → Milvus 后期）
- **异步任务**: BullMQ（Redis）
- **接口**: REST + GraphQL + MCP Server + CLI（nove-cli）+ Skill（nove-skills）

#### AI 服务 nove-ai（实体归属）
- **框架**: FastAPI + Python（AI 生态：LLM 编排、Embedding、图谱、pandas/networkx）
- **能力**: 数据预处理、关联发现、知识图谱、批量总结、用户画像
- **任务形态**: 异步 Job（queued → running → succeeded / failed），结果回写数据库

#### AI 能力轻/重分层
| 层级 | 示例 | 实现 |
|---|---|---|
| 轻量同步 | 实体抽取、字段映射、短文本分类、单场会议摘要 | nove-api 内嵌封装（AiClient） |
| 重量异步 | Embedding 入库、语义关联发现、知识图谱、批量转写总结、用户画像 | nove-ai（Python），Job 化 |

- nove-api 永不直接依赖具体模型，只通过 AiClient（超时 / 重试 / 熔断 / 降级）调用 nove-ai。
- 服务间鉴权：HMAC 签名（X-Timestamp + X-Signature），后期可升级 Service JWT。

#### 人类入口 nove-admin
- **框架**: React + Vite + TypeScript + Ant Design
- 数据查看、数据源管理、权限配置、审计查询、系统管理

### 5.2 技术选型对比

| 方案 | 适用 | 推荐度 |
|---|---|---|
| NestJS（业务）+ FastAPI（AI） | 数据仓库 + AI 预处理，双后端解耦 | ⭐⭐⭐⭐⭐ |
| 单后端承载全部 | 早期 MVP、AI 需求轻 | ⭐⭐⭐ |
| 微服务 | 大型项目、多团队协作 | ⭐⭐⭐ |

### 5.3 核心技术要求
- **多源接入**：统一连接器 + 插件化扩展
- **异步调度**：转写、总结、批量预处理等长时任务
- **权限控制**：人机统一 RBAC + ABAC，细粒度数据访问控制
- **多协议**：REST / GraphQL / MCP / Skill / CLI
- **可观测性**：性能监控、错误追踪、AI 行为审计

---

## 6. 里程碑与计划（12 周）

### 第1-2周（M1）：基础架构与核心数据接入
- **需求澄清与技术选型**：确认数据仓库定位与数据源清单（会议、订单、用户）
- **基础架构**：NestJS + Prisma + PostgreSQL + Redis 骨架；人机统一权限模型初版
- **会议数据接入**：腾讯会议 / 飞书 Webhook + API，转写 / 摘要 / 行动项入库
- **交付物**：项目骨架、会议数据接入链路、基础权限与审计

### 第3-4周（M2）：数据仓库与 AI 预处理
- **仓库格式**：内置数据仓库格式定义（数据模型、元数据、溯源）
- **AI 预处理**：nove-ai（FastAPI）起步，自动总结、实体抽取；AiClient 封装（超时/重试/熔断/降级）
- **异步任务引擎**：BullMQ Job 模型（queued → running → succeeded / failed）
- **交付物**：数据仓库核心模型、AI 预处理链路、Job 引擎

### 第5-6周（M3）：接口层与人机权限
- **REST / GraphQL API**：数据访问与操作接口（OpenAPI 契约驱动）
- **人机统一权限**：AI 独立身份（API Key / scope）、RBAC + ABAC、全量审计
- **nove-cli / nove-skills**：命令行与技能化访问雏形
- **交付物**：开放 API、人机权限体系、CLI / Skill 雏形

### 第7-8周（M4）：MCP 与关联引擎
- **MCP Server**：以 MCP 暴露数据仓库，供 LLM / Agent 直接调用
- **关联引擎**：Embedding 入库、跨数据源关联发现（"用户 ↔ 会议 ↔ 订单"）
- **开发者工作台**：数据源管理、权限配置、审计查询雏形
- **交付物**：MCP 服务、关联引擎初版、开发者工作台

### 第9-10周（M5）：nove-admin 与用户体验
- **nove-admin 开发**：React + Ant Design，数据查看 / 操作 / 审计界面
- **核心功能页面**：数据全景、数据源管理、权限配置、AI 行为审计
- **实时交互**：数据接入状态、任务进度可视化
- **交付物**：nove-admin、核心功能页面

### 第11周（M6）：测试优化与监控
- **内测与反馈**：用户内测、问题收集与优化迭代
- **运营看板**：数据接入量、接口调用、AI 任务、审计事件监控
- **安全加固**：漏洞扫描、权限测试、数据安全加强
- **交付物**：测试报告、监控看板、安全优化方案

### 第12周（Release）：发布与交付
- **正式发布**：v1.0 版本发布，生产环境部署
- **文档沉淀**：架构文档、运维手册、开发指南、接口文档
- **后续规划**：v1.x 版本规划、技术演进路线图
- **交付物**：正式产品、完整文档体系、运维支撑体系

---

## 7. 组织与分工

- 项目负责人（Owner）：杨仕明（目标范围、里程碑、对外沟通）
- 技术负责人/后端：叶俊（架构 / 数据接入 / 权限体系）
- 后端/集成：明轩（数据接入与接口实现、分支协同）
- AI 服务：1 人（nove-ai：预处理、关联引擎、Job）
- 前端：1 人（nove-admin、鉴权与可观测）
- 产品/文档：1 人（需求澄清、原型、验收标准、手册）
- 运维/安全：0.5 人（权限、审计、告警、灰度与发布）

---

## 8. 交付物（v1.0）

### 8.1 核心产品交付
- **nove-admin 应用**：用户与 AI 身份管理、数据查看、数据源管理、权限配置、审计查询
- **开放接口**：REST / GraphQL / MCP / CLI / Skill

### 8.2 技术交付物
- **后端服务与 API 文档**：RESTful API（NestJS）、OpenAPI 3.0 契约、SDK 与集成示例
- **异步任务系统**：会议转写、AI 预处理、批量关联分析
- **数据库与模型设计**：PostgreSQL 表结构与关系、Prisma 模型与迁移、向量索引、数据字典

### 8.3 运营支撑交付
- **监控与运维**：性能监控看板、健康检查与告警、日志聚合、备份与灰度发布
- **数据分析**：数据质量分析、关联覆盖率、接口使用统计、AI 任务效果追踪

### 8.4 安全与合规交付
- **权限与安全**：人机统一权限模型、数据脱敏、API 频率限制、审计日志与溯源
- **合规文档**：数据隐私保护政策、信息安全管理制度、备份与恢复预案、第三方集成安全评估

### 8.5 文档与培训交付
- **技术文档**：架构设计、部署运维手册、API 集成指南、数据库维护指南
- **用户文档**：产品使用手册、管理员操作指南、常见问题、培训材料

---

## 9. 成功指标（KPI）

### 9.1 技术性能指标
- **数据接入可靠性**：核心数据源（会议 / 订单 / 用户）接入成功率 ≥ 99%
- **系统响应性能**：API 平均首响 < 300ms，99% 分位数 < 1s
- **系统可用性** ≥ 99.5%，故障恢复时间 < 5min
- **接口吞吐**：峰值 QPS > 500

### 9.2 业务效果指标
- **数据汇聚覆盖**：核心数据源 100% 接入，自动沉淀率 ≥ 90%
- **AI 预处理覆盖率**：入库数据自动预处理（总结 / 抽取）覆盖率 ≥ 80%
- **权限违规** = 0，敏感信息泄露 = 0，审计记录完整率 = 100%
- **Agent 调用**：≥ 3 个外部 Agent 通过 API / MCP / Skill 稳定调用

### 9.3 用户体验指标
- **管理员满意度** ≥ 4.2/5（内测问卷）
- **数据时效**：实时数据源入库延迟 < 5min，定时数据源按计划完成
- **自定义接入**：新增一类数据源接入耗时 ≤ 1 周（插件化后 ≤ 2 小时）

### 9.4 运营效率指标
- **数据质量**：重复数据率 ≤ 5%，数据完整性 ≥ 95%
- **成本控制**：AI 模型调用成本/月 ≤ 预算目标，资源利用率 ≥ 75%

---

## 10. 风险与应对

- 数据源接入复杂度高 → 契约优先、统一连接器、插件化、沙箱数据先通
- LLM 效果波动 → 离线评测集、结构化输出、可追溯引用、降级兜底
- AI 访问权限风险 → 人机统一权限、最小 scope、全量审计、数据脱敏
- 资源节奏与并行冲突 → 分支并行、场景优先级、双周可交付
- 技术选型演进 → 模块边界清晰、接口稳定、可替换实现
- 数据敏感与合规 → 最小权限、脱敏、审计闭环、分级授权

---

## 11. 数据安全与合规

- 授权：人机统一 RBAC + ABAC；AI 独立身份与 scope 白名单；最小可见与分级授权
- 审计：全链路日志（人类与 AI）；操作留痕与溯源
- 存储：敏感数据加密、密钥托管；对象存储与备份策略
- 传输：TLS 全面加密；Webhook / 订阅验签；服务间 HMAC
- 数据治理：版本与溯源、保留与销毁政策、AI 派生数据可重建

---

## 12. 预算与资源（估算）

- 人力：3–5 人 × 3 个月
- 基础设施：数据库 / 向量库 / 日志监控 / 对象存储
- 第三方：飞书开放平台、腾讯会议、语音转写 / 模型调用
- 费用：按自研 / 云服务策略细化，随里程碑滚动评估

---

## 13. 迭代策略

- 路线：核心数据源接入 → AI 预处理与关联 → 多协议接口 → 人机权限与审计 → 平台化
- 节奏：双周评审与验收；AI 拟定初稿 + 人工校对入库
- 优化：持续改进数据质量与关联效果，数据与能力闭环进化

---

## 14. 附录

### 14.1 历史沿革
- v1.0（2025-09）："实验室 AI 智脑与教育中台"版本，以陆向谦实验室教育场景为切入点。
- v2.0（当前）：定位收敛为"个人 / 组织 / 企业数据仓库与 Agent 数据基础设施"，教育场景作为首个落地用例保留。

### 14.2 术语
- 数据仓库（Data Warehouse）：统一汇聚、存储组织多源数据的系统。
- 人机统一权限：人类用户与 AI Agent 均作为独立访问主体的权限体系。
- MCP：Model Context Protocol，模型上下文协议，供 LLM / Agent 调用数据与工具。
- Skill：面向 Agent 的技能化接口封装（如 nove-skills）。
- RAG：Retrieval-Augmented Generation，检索增强生成。
- HMAC：Hash-based Message Authentication Code，服务间请求签名鉴权。
